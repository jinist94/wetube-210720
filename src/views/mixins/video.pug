mixin video(info)
    div
        h4
            a(href=`/videos/${info.id}`)=info.title
        p=info.description
        small=info.createAt
        ul 
            each hashtag in info.hashtags 
                li=hashtag
        hr